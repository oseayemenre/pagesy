version: '3'

services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=pagesy
      - POSTGRES_PASSWORD=pagesy_password
      - POSTGRES_DB=pagesy_db
    volumes:
      - db:/var/lib/postgresql/data

volumes:
  db:
